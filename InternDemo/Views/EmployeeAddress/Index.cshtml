@model IEnumerable<InternDemo.Models.EmployeeAddress>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

@*<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.city)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.district)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.wardNo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employee)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.city)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.district)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.wardNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.fristName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.addressId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.addressId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.addressId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>*@

    <div class="content-wrapper">
        <section class="content-header">
            <div class="container-fluid">
                <div class="row mb-2">
                    <div class="col-sm-6">
                        <h1>Employee Address</h1>
                    </div>
                    <div class="col-sm-6">
                        <ol class="breadcrumb float-sm-right">
                            <li class="breadcrumb-item"><a href="#">Home</a></li>

                        </ol>
                    </div>
                </div>
            </div><!-- /.container-fluid -->
        </section>
        <section class="content">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-12">
                        <div class="card">
                           
                            <!-- /.card-header -->
                            <div class="card-body">
                                <table class="tablemanager">
                                    <thead>
                                        <tr>
                                            <th>Employee Name</th>
                                            <th>City</th>
                                            <th>District</th>
                                            <th>Ward Number</th>
                                            <th class="disableFilterBy">Action</th>

                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var item in Model)
                                        {
                                        <tr>
                                            <td>  @Html.DisplayFor(modelItem => item.Employee.FristName) @Html.DisplayFor(modelItem => item.Employee.MiddleName) @Html.DisplayFor(modelItem => item.Employee.LastName)</td>
                                           ,
                                            <td> @Html.DisplayFor(modelItem => item.City)</td>
                                            <td> @Html.DisplayFor(modelItem => item.District)</td>
                                            <td>  @Html.DisplayFor(modelItem => item.WardNo)</td>

                                            <td>
                                                <a asp-action="Edit" class="btn btn-success btn-sm rounded-0" asp-route-id="@item.AddressId">Edit</a>
                                                <a asp-action="Delete" class="btn btn-danger btn-sm rounded-0" asp-route-id="@item.AddressId">Delete</a>
                                            </td>

                                        </tr>
                                        }

                                    </tbody>

                                </table>
                            </div>
                            <!-- /.card-body -->
                        </div>

                        <!-- /.card -->
                    </div>
                    <!-- /.col -->
                </div>
                <!-- /.row -->
            </div>
            <a asp-action="Create" class="btn btn-block btn-default btn-lg">Create New</a>
            <!-- /.container-fluid -->
        </section>
    </div>